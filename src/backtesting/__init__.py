"""Portfolio backtesting on model predictions.

``signals`` is pure-pandas and importable in the main thesis env. ``vbt_adapter``
and ``basket`` require vectorbt and must run in the isolated backtest env
(see requirements-backtest.txt).
"""
